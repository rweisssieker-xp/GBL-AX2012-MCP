# Ignore all dot-folders (folders starting with .)
.**/

# But keep these important files
!.gitignore
!.gitattributes
!.github/
!.github/**

# Ignore Docker files (not part of MCP)
Dockerfile
docker-compose.yml
*.dockerignore